# 科研项目预算管理系统

科研经费的全生命周期预算管理:初始预算编制与审批、业务记录(支出)登记、预算调整、执行台账与统计。本文件是领域术语的权威词汇表,只收术语,不写实现。

## Language

### 核心域

**项目 (Project)**:
一笔科研经费的预算管理单元,有编号、名称、预算总额与年度预算。
_Avoid_: 课题、经费卡

**预算科目 (BudgetSubject)**:
项目内按层级组织的支出分类;只有叶科目可直接记账。
_Avoid_: 科目项、预算项

**初始预算 (Initial Budget)**:
项目获批后编制、经审批生效的首套科目预算;生效前是草稿申请。
_Avoid_: 原始预算、预算申请(泛称时)

**业务记录 (Business Record)**:
一笔实际支出在某叶科目下的登记,含金额、业务发生日期、经办人、状态与单据编号。
_Avoid_: 流水、支出记录

**单据编号 (docNo)**:
财务系统中该笔业务的原始单据号;项目内以它查重,可为空。
_Avoid_: 凭证号

**作废 (Void)**:
业务记录的终态之一,保留记录与历史痕迹、不再计入执行统计;非物理删除。
_Avoid_: 删除、撤销

**执行台账 (Ledger)**:
项目各科目「预算 vs 已执行」的实时视图。
_Avoid_: 报表(泛称时)

**到账流水 (Receipt Record)**:
经费入账的记录,与支出(业务记录)相对。
_Avoid_: 收入记录

**预算调整 (Adjustment)**:
科目间预算额度的变更,走编制→审批流程,生效后留痕。
_Avoid_: 调账、改预算

### 导入

**导入批次 (Import Batch)**:
一次 Excel 导入的两阶段载体:解析预览(可暂存)→ 确认入账;确认前不产生业务记录。
_Avoid_: 上传记录

**结算单导入 (Settlement Import)**:
「财务系统个人结算单查询」Excel 的导入:表头在第 4 行,导入后逐条指定科目;业务退单行不导入。
_Avoid_: 银行流水导入

**收件箱 (Inbox)**:
无人值守导入时,结算单文件在用户主机上的约定投递目录,按项目编号分子目录;处理结果归档至 _done / _failed。
_Avoid_: 上传目录、网盘

### Agent 自动化

**Coding Agent**:
受用户指令驱动、以服务账号身份经 API 操作本系统的代理程序(如 ZCode 会话)。
_Avoid_: 机器人、脚本

**服务账号 (Service Account)**:
coding agent 使用的真实用户档案,与人类用户同受全局角色与项目成员权限约束。
_Avoid_: 机器人账号、API 用户

**个人凭证 (Personal API Key)**:
用户为自己签发的机器凭证,以本人身份操作;权限不高于本人,创建后不可编辑、可撤销、可选有效期。
_Avoid_: 个人 token、访问令牌

**凭证档位 (Key Tier)**:
凭证的权限上限:只读 / 读写 / 完整;实际权限 = 本人权限 ∩ 档位 ∩ 项目范围。
_Avoid_: 权限级别、角色

**机器凭证 (API Key)**:
绑定服务账号的静态密钥,仅创建时明文展示一次,可撤销;经 Bearer 头向服务认证。
_Avoid_: token(泛称时)、密码

**无人值守任务 (Unattended Run)**:
coding agent 在无人在场时按既定指令执行的操作序列(通常由定时指令触发)。
_Avoid_: 后台任务、自动任务

**自主操作**:
agent 无需逐次授权即可执行的操作:查询、统计、文件解析、预览、暂存、科目指派。
_Avoid_: 后台操作

**指令授权操作**:
仅当任务指令明确列出(宜点名对象)时 agent 才可执行的操作:新增/修改业务记录、确认导入、到账登记、预算调整的编制与提交。
_Avoid_: —

**硬排除操作**:
无人值守永不执行(服务端对无人值守凭证强制拒绝)、仅限人在场逐次确认的操作:作废、审批、成员与权限变更、凭证管理。
_Avoid_: 危险操作(泛称)

**科目映射记忆 (Subject Mapping)**:
同项目内「摘要 → 科目」的历史确认结果,用于导入行的自动指派;未命中时由 agent 判断或挂起待指派。
_Avoid_: 规则表
