# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。
版本发布流程见 [AGENTS.md](./AGENTS.md)。

## [Unreleased]

## [0.11.0] - 2026-09-02

### 新增

- **重复数据防护(ADR 0002)**:Excel 导入与录入的重复入账全链路拦截,统一查重判定器覆盖所有写入口(标准模板导入 / 结算单导入 / 手动新增编辑 / coding agent)
  - **硬重复(禁止入账,无强制通道)**:单据编号与项目内未作废记录同号——导入确认 422、录入保存 409(附冲突详情与「先作废旧记录」出路提示)
  - **疑似重复(提示后可强制)**:无编号行按查重指纹(年度+金额+业务日期+摘要归一化,与科目映射记忆同口径)命中——导入预览默认不勾选、可强制导入;录入仅 toast 警示不阻断
  - **数据库兜底**:部分唯一索引 `(project_id, docNo) WHERE … AND is_void = false`(并发/旁路最后防线);**作废即释放编号**,撤销后可重导。迁移内置存量冲突预检,发现旧版「强制导入」遗留的同号数据时给出定位与处置 SQL,不裸阻断部署
  - **标准模板**:末尾追加可选「单据编号」列(老文件向后兼容),列预格式化为文本防 Excel 吞前导零;确认时事务内复查单据编号
  - 预览页重复行分档呈现(硬重复行锁定 + 徽标;疑似行可强制);导入确认/录入的并发竞态由唯一索引兜底并翻译为可读 409/422(原为 500)

## [0.10.0] - 2026-09-02

### 新增

- **Coding Agent 自动化(ADR 0001)**:coding agent 以 API Key 绑定的用户身份经 HTTP API 操作系统,交互与无人值守皆可
  - **机器认证**:`Authorization: Bearer bma_…`(与 SSO/MOCK 会话共存);key 哈希入库、明文仅签发时展示一次、可撤销、可选有效期
  - **硬排除(服务端强制)**:无人值守凭证触碰 作废 / 审批 / 成员管理 直接 403,被拒尝试写入审计(`unattended.denied`)
  - **个人 API 凭证自助管理**(侧边栏「API 凭证」页):任意用户签发/撤销自己的凭证,权限不高于本人
    - 针对性权限(scope 只收窄不放大):档位(只读 / 读写 / 完整)× 项目范围(全部 / 指定)× 有效期;创建后不可编辑,要改即撤销重发
    - 指定项目范围的凭证:跨项目聚合接口(统计、全量审计、审批待办、用户列表等)与未授权项目一律 403(`apikey.denied` 审计),项目列表仅返回授权项目
    - 红线:凭证管理接口拒绝一切 Bearer 凭证调用,杜绝 agent 自我签发
  - **服务账号**:`npm run make-agent`(建账号 / 发 key / 撤销),面向长期无人值守定时任务(身份与个人解耦)
  - **MCP Server**:`npm run mcp`(stdio),17 个 `budget_*` 命名工具,确认策略内嵌工具描述;硬排除动作不提供工具
  - **Agent Skill**:`skills/budget-ops`(HTTP 为准,不依赖 MCP):操作目录、三档确认策略、收件箱无人值守流程(`~/budget-inbox/<项目编号>/`,成功 `_done/` 失败 `_failed/`)
- **科目映射记忆**:`GET /api/projects/:id/subject-mappings` — 项目内「摘要(归一化)→ 科目」历史确认统计,供无人值守导入自动指派科目;未命中由 agent 语义判断或留暂存待指派

## [0.9.0] - 2026-09-01

### 新增

- **业务记录:个人结算单 Excel 导入**:支持直接上传财务系统导出的「个人结算单查询」xlsx(上传自动识别格式,原标准模板导入不受影响)
  - 列映射:单据编号 / 单据状态(完成记账→已支出、制单保存→财务系统审批,业务退单跳过不导入)/ 填制日期→业务发生日期 / 事项→摘要(富文本拍平)/ 金额 / 经办人
  - 无科目列:预览页逐条指定叶科目(可搜索下拉 + 勾选多行批量设置),预算年度默认取填制日期年份、逐行可改
  - **暂存续办**:科目指定等修改即时持久化,可随时退出,从「导入批次」列表继续;确认导入才写入业务记录
  - **查重**:单据编号命中项目内已有记录(或文件内重复)→ 疑似重复默认不导入,可勾选强制导入;确认时事务内兜底复查
- **业务记录新增「单据编号」字段**(非必填):手动新增/编辑可填,台账新增列并支持文本筛选
- **业务记录批量作废**:表格勾选多行(表头全选当前筛选可见行)批量作废,作废原因批量共用,已作废行自动跳过;仅管理员/项目负责人可操作
- **业务记录表头排序**:点击列标题循环 升序→降序→取消,全部业务列可排(科目按名称、金额按数值、单据编号空值恒最后),与筛选独立、单列、不持久化

### 修复

- 作废权限收紧:单条与批量作废仅管理员/项目负责人可执行(此前经办人 HANDLER 也可作废,与「只读成员」角色定义矛盾)

## [0.8.0] - 2026-09-01

### 新增

- **调整单驳回后可编辑并再次提交**:驳回不再是终态——列表「编辑 / 提交审批」修改后重新进入审批流程
  - 详情新增「驳回意见」与「审批记录」区块:每轮 新建/提交/审批/驳回/撤回 的 操作人/时间/意见 完整保留展示(取自审计日志,只增不删)
  - **审批版本绑定**:审批 / 驳回 / 撤回 携带审批人所见版本的提交代,行锁内校验——待审期间单据被驳回 / 再提交时,旧请求 409,杜绝把旧明细套在新提交上执行;编辑与提交对调整单行加锁串行化
  - 再提交刷新提交时间(submittedAt):详情「原预算」基线随最新一轮提交重算
  - 详情页成员管理变更后,「项目负责人」摘要即时刷新

### 修复

- 调整审批表(总预算 / 年度调整表)金额不再按万元两位小数截断:精确换算(12345.67 元 → 1.234567 万),「调整后」列与合计同步
- 自动生成调整原因说明的金额不再取整(精确万元,负数精度经 Decimal 保持)
- 业务记录默认状态筛选纳入「财务系统审批」(状态常量笔误导致该状态记录默认被过滤)

## [0.7.1] - 2026-08-31

### 修复

- **预算调整总维度调减护栏**:现有科目的总预算调整后不得为负——提交与审批双重校验
  - 按科目**净额**计算(同科目多行如 -700/+200 净 -500 不误杀),超调单 422 并提示 现总预算/在途调减/本单调减
  - 并发提交 / 并发审批经涉事科目总预算行锁串行化(统一排序防死锁),不再出现两单互相计入投影后双双卡死的局面
- 自动生成调整原因说明的金额**不再按万元取整**:精确换算(如 12345.67 元 → 1.234567 万元),负数精度经 Decimal 保持,无浮点伪影

## [0.7.0] - 2026-08-28

### 新增

- **项目管理:编辑与归档/恢复**
  - 编辑入口两处:列表行「编辑」+ 详情页「编辑项目信息」,共用同一弹窗(react-hook-form + zod);编号创建后不可改,负责人变更暂不支持
  - 「删除」= 归档(软删除):数据完整保留、列表隐藏,「显示已归档」开关查看(灰显 + 徽标),一键「恢复」;归档/恢复均记审计
  - **归档期间项目只读**:一切写路径(业务记录 / 回款 / 预算调整 / 初始编制 / 导入 / 审批 / 跨年结转)在权限层统一 409,详情页隐藏全部录入按钮并显示已归档徽标——恢复前数据冻结
  - 列表与详情展示「项目负责人」(从当前 OWNER 成员派生,成员管理变更后即时刷新)
  - 权限沿用 project:edit(ADMIN 或该项目 OWNER);列表行级下发 canEdit 门控按钮

### 修复

- 项目编辑审计快照覆盖全部可编辑字段(此前只记 名称/级别/备注,改承担单位/起止时间在操作日志里看不出变化)
- 项目起止时间日期按日历日解析与序列化,任意时区打开/保存不再偏移一天

## [0.6.0] - 2026-08-27

### 新增

- **预算调整双模式:调剂 / 追加下达**
  - 追加下达(ALLOCATE):只做正向分配、合计须 > 0(不再要求零和),可选「新经费入账」——审批通过同步调增科目总预算与项目总预算(人才类项目分批到账、最初不告知总额的场景)
  - 新增科目行:审批通过自动建档(叶科目)并按首笔分配额立账,可选择挂靠父节点;目标年份未建账时自动创建年度账
  - 护栏:现有科目行的分配额 ≤ 科目总预算 − 历年已分配合计(剩余可分配额);「新经费入账」按自身守恒校验、不受原护栏限制
  - 并发安全:同项目追加审批按项目预算行串行化,双重审批恰好一个生效(另一个 409)
- **调整单在线预览**:调整列表操作栏「总预算」「年度预算」直接打开预览窗口——页式审批表渲染、维度可切换,窗口内打印 / 下载(与导出接口同源字节一致);失败有明确提示并可重试
  - 底座 file-viewer(preset-office),Worker/WASM/字体资产本地自托管(`npm install` 自动生成 `public/file-viewer`,只保留 Word/PDF/xlsx 约 12MB),预览全程无外网
- **审批中心详情 + 就地办理**:预算调整待办可查看完整明细(每行 原预算/调整额/调整后 双维度金额与合计,同一科目多行自动聚合,双维度原因、申请人、类型徽标),并在详情内直接 审批 / 驳回(驳回必填意见),成功后列表即时刷新;项目内「明细」与审批中心共用同一组件
  - 待审单的「原预算」为提交时刻快照,不被提交后他单审批污染;本单新设科目原值恒为 0
- **调整列表筛选**:科目模糊检索(名称/编号)下拉;表单内科目 / 父节点下拉升级为可输入筛选的 Combobox,长科目名截断不再撑宽表格
- **总预算审批表导出全部科目**:总预算维度覆盖审批时点目录内全部叶科目(未调整科目成行 基线/0.00/基线),行序按科目表顺序固定,重复导出内容稳定;新设科目行不重复、原值归零

### 修复

- 导出审批表金额翻倍:汇总行账本漂移(current ≠ initial + adjustment)时拒绝生成文书并提示用 `scripts/recalc-summary-budgets.ts` 修复;导出「原预算」按基线重建,不受本单及后续审批污染
- 附件预览统一切换 file-viewer 底座(移除 `<img>/<iframe>` 分叉),渲染失败不再停留加载态,提示并可下载原文件;类型白名单不变(图片 / PDF)
- 审批中心调整单「原因」列此前恒显示"—"(读取了接口从未下发的字段),现按 总/年 双维度摘要展示
- Docker 镜像构建修复:postinstall 资产生成在 deps 阶段可用、构建期通过环境变量校验、alpine 下 Prisma 引擎探测(openssl3)

## [0.5.0] - 2026-08-21

### 新增

- **经费余额统计**(统计分析页第 4 个 tab):总预算口径结余分析——科目总预算(含调整生效额) − 累计占用,此前系统仅有年度/项目口径,科目总预算口径无处可查
  - 行 = 项目 × 科目,合计行按命中科目**去重叶集合**计算(父子科目同时命中不重复计数)
  - **科目模糊匹配**:名称/编号 `contains`(不区分大小写),跨全部项目;命中非叶科目(如"直接费")按后代叶汇总并标注"(含下级)"
  - 年度可选:选定后追加 年度预算/年度占用/年度结余 三列(非叶行同样按后代叶汇总);列由"已应用的查询"驱动
  - "仅看结余为负"快捷开关,定位风险科目;无预算但有支出的科目显示负结余
  - 默认按总结余升序(最紧张在前),列头可点排序,负数红色显示;支持 xlsx 导出(合计行与明细列纵向对齐)
- **自定义统计科目模糊检索**:科目筛选从"先选项目 → 叶科目精确下拉"升级为模糊输入框(跨项目可用),检索业务明细不再需要先选项目
- 新增 `GET /api/statistics/balance`;`/api/statistics/custom|export` 的 `subjectId`(精确 UUID)参数替换为 `subject`(模糊串)

## [0.4.0] - 2026-08-21

### 新增

- **业务记录报销凭证附件**:每笔业务可上传报销凭证附件(图片 / PDF / Office 文档,单文件 ≤ 50MB,不限数量),便于后期整理经费报告
  - 创建/编辑业务时可一并附带附件;已保存业务可在记录页「附件」抽屉中查看 / 补充 / 删除
  - 附件以二进制存入数据库,随库备份;权限沿用业务记录编辑权(上传 / 删除)与项目查看权(列表 / 下载 / 导出)
  - **附件在线预览**:附件抽屉内直接预览(图片 / PDF 等按浏览器原生能力渲染,零额外依赖)
- **按预算科目层级打包附件**:记录页工具栏「按科目打包」,zip 内按预算目录层级(编号_名称)组织文件夹,整理经费报告即取即用
  - 文件名模板可自定义,占位符:`{date}` `{amount}` `{handler}` `{subject}` `{summary}` `{status}` `{year}` `{original}`(默认 `{date}_{amount}_{summary}_{original}`),配置弹窗实时预览
  - 可按年度筛选打包范围;同文件夹重名自动追加 (1)/(2) 去重
- 新建项目起止时间支持**手动键入**(`yyyy-MM-dd`,兼容 `/` `.` 分隔;区间两日期倒序自动交换)
- 新增业务记录的科目下拉优化:选项只显示科目名称(不再带编号),支持**按名称或编号输入筛选**;存在同名科目时自动追加编号消歧

### 修复

- 初始预算编制:中文 IME 输入被打断(输一个字符即跳出输入框)
- 初始预算编制:数量 / 单价 / 总预算数字输入被打断;单元格交叉编辑焦点丢失
- 初始预算编制:保存草稿后再修改,「保存草稿」按钮消失
- 初始预算编制:表格草稿输入中刷新 / 关闭页面无离开确认,草稿静默丢失
- 金额输入框聚焦中按 Enter 提交表单会提交旧值(编辑场景会静默提交旧金额)
- 日期选择器:点击输入框后焦点被日历弹层抢走,无法直接键入
- 保存草稿后再修改会清零科目层级,导致预算调整页「发起调整」整页崩溃(`Invalid count value: -1`)
- 附件预览竞态时误 revoke 飞行中的 blob URL;开发服务器 workspace root 误判(多 lockfile 场景)

## [0.3.0] - 2026-08-06

### 新增

- **Authentik SSO 登录**:登录页 + OIDC 授权码流程(PKCE/state/nonce),`openid-client` + `jose` 手写集成,会话为 HttpOnly JWT cookie(8h)
- 首次 SSO 登录自动建档(JIT,默认普通用户);`npm run make-admin -- <用户名>` 提升首个管理员
- 项目成员管理:管理员在项目概览页增删成员、调整角色(负责人=全部权限 / 成员=可录入),全程审计留痕
- 新建项目可指定负责人(自动获得 OWNER 成员编辑权)
- 顶栏用户菜单(姓名 + 角色 + 退出登录,联动 Authentik end-session)
- **统一业务录入页**(`/records`,侧边栏「业务录入」):跨项目录入卡片(项目 → 叶科目级联,连续录入)+ 全局记录列表(默认可录入项目,可切全部只读),行内修改/作废
- 新增 `GET /api/me/projects`:全部项目 + 当前用户权限标记(统一页数据源)
- 成员管理与级联选择改可搜索 Combobox(新增 cmdk Command/Combobox 组件)
- **业务记录 Excel 式表头筛选**(两页):每列表头漏斗(值清单勾选 / 文本包含 / 金额范围 / 日期范围);日期筛选含快捷预设(今天/最近7天/本月/本季度/本年等)+ 起止日期输入框 + 日历拖选

### 变更

- **角色模型收敛(破坏性)**:全局角色改为 `ADMIN` / `USER` 两级;项目编辑权改由 `ProjectMember(OWNER)` 驱动——存量 `PROJECT_OWNER` / `AUTHORIZED_HANDLER` 用户迁移为 `USER`,并按 `Project.ownerId` 自动回填 OWNER 成员行
- **HANDLER 成员可录入**:`record:create/edit/void` 对 OWNER+HANDLER 放行(录入人员=HANDLER);预算编制/调整/导入仍 OWNER 专属;项目详情随下发 `canEdit` + `canWriteRecords`
- **普通用户全局只读**:全部项目的台账/记录/统计/审计日志可见,编辑动作服务端 403
- 查看态 UI 门控:无编辑权时隐藏/禁用新建项目、新增记录、发起调整、保存、导入等入口
- `MOCK_AUTH` 开关保留:本地开发/测试继续用 mock 身份;`false` 时启用 SSO 并强制要求 OIDC 环境变量
- `/api/users` 在 SSO 模式下仅管理员可用(`user:list`);`excel-template` 下载补充登录校验

### 修复

- 审批待办接口由原始角色比较改为权限矩阵校验
- 调整草稿编辑/删除补齐项目级权限校验(此前只查全局角色)
- 业务记录页筛选与变更刷新共用同一查询(修掉筛选变化后列表卡加载态)
- 日历月份切换箭头被网格覆盖(加 z-10)
- cookie Secure 标记按 APP_BASE_URL 协议推导(修 http 生产部署登录断链)

## [0.2.0] - 2026-08-05

### 新增

- 侧边栏支持收缩为图标窄栏（cookie 持久化，首屏无闪烁），底部展示版本号
- 新增「更新日志」页面（`/changelog`）与侧边栏入口，渲染本文件
- 初始预算编制：无草稿时默认套用 18 项预设科目模板 + 当年年度行，附「清空科目」出口
- 执行台账叶科目可点击，跳转业务记录页并自动筛选（`subjectId` + `year`）
- 业务记录支持连续录入（保存并继续新增）
- 亮 / 暗双主题切换（next-themes）

### 变更

- **全量 UX 重构**：antd 完全移除，迁移至 shadcn/ui + Tailwind CSS 4（DESIGN.md / Vercel 设计语言 token 体系）
- 项目详情页改为 Tab 子导航，侧边栏精简；「年初预算」更名「初始预算编制」
- 树形台账表用 TanStack Table 重写，支持列显隐
- 预算编辑器改为显式保存 + 脏状态追踪，离开拦截覆盖浏览器关闭/刷新与站内导航
- 预算调整原因改用原生受控 textarea，根治中文输入法被打断
- 统计分析改为网格查询构建器，科目随项目即时加载

### 修复

- 脏状态下 SPA 站内导航（项目 Tab / 侧边栏）绕过离开拦截导致草稿丢失（PR #1 codex review）

## [0.1.0] - 2026-07-29

### 新增

- 初始版本：项目管理、预算编制与审批、双维度预算调整、业务记录、执行台账、到账流水、Excel 导入、年度结转、统计分析、操作日志（antd 5 界面）
