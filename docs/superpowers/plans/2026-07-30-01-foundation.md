# 地基层(Foundation)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建全系统依赖的可测试地基——Next.js 全栈脚手架、Docker 化 Postgres、全量 Prisma 数据模型、金额/占用/执行率纯公式库、统一审计中间件、mock 鉴权与应用层 RBAC 守卫。

**Architecture:** Next.js(App Router)全栈单体;Prisma + PostgreSQL(定点金额);业务逻辑放在 `services/`,纯公式集中在 `lib/budget/`,路由层只管 HTTP;mock 鉴权 + 自写 RBAC 守卫;审计走 service 层之上的拦截中间件,日志与业务同事务写入。

**Tech Stack:** Next.js 14(App Router)、React 18、TypeScript 5(strict)、Ant Design 5、Prisma 5、PostgreSQL 16、decimal.js、Zod、Vitest、ESLint + Prettier + Husky + lint-staged + commitlint。

## Global Constraints

- 金额:数据库一律 `@db.Decimal(18,2)`;后端领域计算用 `decimal.js` 的 `Decimal`;JSON 接口传输金额一律用字符串;**前端只展示,不参与预算计算**。
- 主键:所有表主键用 UUID v7,应用层生成(`lib/id.ts` 的 `uuidv7()`)。
- 金额计算禁止使用 JS 原生 `number` 做任何加减乘除预算运算(展示用 `Intl.NumberFormat` 格式化字符串除外)。
- TypeScript `strict: true`,禁止 `any`(金额相关类型尤其严格)。
- 代码风格:ESLint + Prettier;提交前 Husky pre-commit 跑 lint-staged;提交信息遵循 commitlint `feat:/fix:/chore:` 等约定。
- 鉴权:首版 mock(`MOCK_AUTH=true`),但权限守卫代码按真实用户编写,当前用户来源为 mock。

---

## File Structure(地基层产出的文件职责)

```
budget-management/
├── docker-compose.yml              # dev: app + postgres 两容器
├── Dockerfile                      # prod 镜像(多阶段构建)
├── .env.example                    # 环境变量模板(进 git)
├── .env                            # 本地真值(gitignore)
├── package.json
├── tsconfig.json                   # strict: true
├── next.config.mjs
├── .eslintrc.cjs / eslint.config.mjs
├── .prettierrc
├── commitlint.config.cjs
├── .husky/pre-commit               # lint-staged
├── prisma/
│   └── schema.prisma               # 全量数据模型(§16)
├── src/
│   ├── app/                        # Next.js App Router
│   │   ├── layout.tsx              # 根布局 + ConfigProvider 紫色主题
│   │   ├── page.tsx                # 占位首页
│   │   └── (暂无业务路由,后续计划)
│   ├── lib/
│   │   ├── env.ts                  # Zod 校验的环境变量单例
│   │   ├── id.ts                   # uuidv7 生成
│   │   ├── decimal.ts              # decimal.js 配置 + Prisma Decimal 互转
│   │   ├── prisma.ts               # PrismaClient 单例
│   │   ├── budget/                 # 纯公式库(无副作用,核心)
│   │   │   ├── money.ts            # Decimal 运算封装
│   │   │   ├── occupancy.ts        # §4.5 占用/应付未付/已支出
│   │   │   ├── rate.ts             # §4.5 执行率(含除零)
│   │   │   ├── adjustable.ts       # §7.4 可调额度/可操作额度
│   │   │   ├── rollup.ts           # §4.4/11 父节点上卷
│   │   │   └── index.ts
│   │   └── auth/
│   │       ├── session.ts          # mock 当前用户
│   │       ├── permissions.ts      # RBAC 矩阵 + requirePermission
│   │       └── projects.ts         # getAccessibleProjectIds
│   ├── server/
│   │   ├── audit/
│   │   │   ├── decorator.ts        # @Auditable 标记
│   │   │   ├── interceptor.ts      # 审计拦截(事务内写 audit_logs)
│   │   │   └── snapshot.ts         # before/after 整行 JSONB 快照
│   │   └── services/               # 业务 service(后续计划填充)
│   └── components/
│       └── ui/                     # 业务封装组件(后续计划填充)
└── tests/
    ├── lib/budget/                 # 公式库单测
    └── server/                     # service 集成测
```

每个文件单一职责:公式库按"占用/执行率/可调额度/上卷"拆分;鉴权按"会话/权限/项目"拆分;审计按"标记/拦截/快照"拆分。

---

## Task 1: 初始化 Next.js 项目与基础依赖

**Files:**
- Create: `package.json`(由 create-next-app 生成后补充依赖)
- Create: `tsconfig.json`、`next.config.mjs`、`src/app/layout.tsx`、`src/app/page.tsx`
- Create: `.gitignore`(更新已有)

**Interfaces:**
- Consumes: 无(起点)
- Produces: 可运行的空 Next.js 项目;`npm run dev` 启动成功。

- [ ] **Step 1: 用 create-next-app 初始化(TS + App Router + ESLint + src/ 目录)**

Run:
```bash
cd /home/z/codebase/budget-management
npx create-next-app@latest . --typescript --app --eslint --src-dir --import-alias "@/*" --use-npm --no-tailwind
```
提示是否在非空目录继续时选 Yes。`--no-tailwind` 因为我们用 AntD。

Expected: 生成 `package.json`、`tsconfig.json`、`src/app/layout.tsx` 等。

- [ ] **Step 2: 安装核心运行时依赖**

Run:
```bash
npm install antd@^5 decimal.js@^10 zod@^3 @prisma/client@^5
npm install -D prisma@^5 vitest@^1 @vitest/ui
```

Expected: `package.json` dependencies 与 devDependencies 更新。

- [ ] **Step 3: 把 strict 确认为开**

修改 `tsconfig.json`,确保 `compilerOptions.strict: true`(create-next-app 默认已开,核对即可)。

- [ ] **Step 4: 验证项目能跑**

Run:
```bash
npm run build
```
Expected: 构建成功(可能 warn 无静态内容,正常)。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: init next.js app with antd, prisma, decimal.js, zod, vitest"
```

---

## Task 2: 代码质量工具链(ESLint + Prettier + Husky + lint-staged + commitlint)

**Files:**
- Create: `.prettierrc`、`commitlint.config.cjs`、`.husky/pre-commit`
- Modify: `package.json`(加 scripts 与 lint-staged 配置)、ESLint 配置(create-next-app 已生成)

**Interfaces:**
- Consumes: Task 1 的项目
- Produces: `npm run lint`、`npm run format`、`npm run check-types` 可用;提交触发 pre-commit。

- [ ] **Step 1: 安装工具依赖**

Run:
```bash
npm install -D prettier eslint-config-prettier eslint-plugin-prettier husky lint-staged @commitlint/cli @commitlint/config-conventional
```

- [ ] **Step 2: 配置 Prettier**

Create `.prettierrc`:
```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100,
  "tabWidth": 2
}
```

- [ ] **Step 3: 关掉 ESLint 与 Prettier 的冲突规则**

Modify ESLint 配置(create-next-app 生成的是 `eslint.config.mjs` 或 `.eslintrc.json`),在 `extends`/配置数组中加入 `"prettier"`(即 `eslint-config-prettier`),并追加规则:
```js
rules: {
  "@typescript-eslint/no-explicit-any": "error",
  "no-restricted-syntax": [
    "error",
    {
      selector: "BinaryExpression[operator='+'[arguments...]",
      message: "金额计算禁止用 + 拼接;使用 decimal.js。详见 lib/decimal.ts"
    }
  ]
}
```
(注:`no-restricted-syntax` 的 selector 用于代码审查提醒,实际金额防护靠类型与单测;若该 selector 语法在版本间不稳,退化为只在 review 中检查,不阻塞构建。)

- [ ] **Step 4: 配置 commitlint**

Create `commitlint.config.cjs`:
```js
module.exports = { extends: ['@commitlint/config-conventional'] };
```

- [ ] **Step 5: 配置 package.json scripts 与 lint-staged**

Modify `package.json`,追加:
```json
{
  "scripts": {
    "lint": "next lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md,css}": ["prettier --write"]
  }
}
```

- [ ] **Step 6: 初始化 Husky 并写 pre-commit 钩子**

Run:
```bash
npx husky install
npx husky add .husky/pre-commit "npx lint-staged"
chmod +x .husky/pre-commit
```

- [ ] **Step 7: 验证工具链**

Run:
```bash
npm run format
npm run lint
npm run check-types
```
Expected: 全部通过(无 error)。

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "chore: setup eslint prettier husky lint-staged commitlint"
```

---

## Task 3: Docker 化开发环境(app + postgres)

**Files:**
- Create: `docker-compose.yml`
- Create: `Dockerfile`(多阶段,prod 用)
- Create: `.dockerignore`

**Interfaces:**
- Consumes: Task 1 的项目
- Produces: `docker compose up` 起一个 postgres(app 用本地 npm run dev 连);`Dockerfile` 可 build 出 prod 镜像。

- [ ] **Step 1: 写 docker-compose.yml(只放 postgres + 可选 app 注释)**

Create `docker-compose.yml`:
```yaml
services:
  postgres:
    image: postgres:16-alpine
    container_name: budget-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: budget
      POSTGRES_PASSWORD: budget
      POSTGRES_DB: budget
    ports:
      - "5432:5432"
    volumes:
      - budget-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U budget -d budget"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  budget-pgdata:
```

- [ ] **Step 2: 写 .dockerignore**

Create `.dockerignore`:
```
node_modules
.next
.git
.env
*.log
```

- [ ] **Step 3: 写 Dockerfile(多阶段 prod)**

Create `Dockerfile`:
```dockerfile
# deps
FROM node:20-alpine AS deps
WORKDIR /app
COPY package*.json ./
RUN npm ci

# builder
FROM node:20-alpine AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npx prisma generate
RUN npm run build

# runner
FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production
COPY --from=builder /app/public ./public
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
COPY --from=builder /app/prisma ./prisma
EXPOSE 3000
CMD ["node", "server.js"]
```

- [ ] **Step 4: 验证 postgres 能起**

Run:
```bash
docker compose up -d postgres
docker compose ps
```
Expected: postgres 状态 healthy。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: add docker-compose postgres and prod dockerfile"
```

---

## Task 4: 环境变量单例(lib/env.ts)

**Files:**
- Create: `src/lib/env.ts`
- Create: `.env.example`
- Create: `.env`(gitignore 已含)

**Interfaces:**
- Consumes: `process.env`
- Produces: `env` 对象,字段:`DATABASE_URL: string`、`NODE_ENV: 'development'|'production'|'test'`、`MOCK_AUTH: boolean`、`APP_PORT: number`。其他模块 `import { env } from '@/lib/env'`。

- [ ] **Step 1: 写 .env.example**

Create `.env.example`:
```env
# Database
DATABASE_URL="postgresql://budget:budget@localhost:5432/budget?schema=public"

# Runtime
NODE_ENV=development
APP_PORT=3000

# Auth (V1 mock; switch to real auth later)
MOCK_AUTH=true
```

- [ ] **Step 2: 复制为 .env(本地真值)**

Run:
```bash
cp .env.example .env
```

- [ ] **Step 3: 写 lib/env.ts(Zod 校验)**

Create `src/lib/env.ts`:
```ts
import { z } from 'zod';

const envSchema = z.object({
  DATABASE_URL: z.string().url(),
  NODE_ENV: z.enum(['development', 'production', 'test']).default('development'),
  APP_PORT: z.coerce.number().default(3000),
  MOCK_AUTH: z
    .string()
    .transform((v) => v === 'true')
    .default('true'),
});

const parsed = envSchema.safeParse(process.env);

if (!parsed.success) {
  console.error('❌ Invalid environment variables:', parsed.error.flatten().fieldErrors);
  throw new Error('Invalid environment variables. See errors above.');
}

export const env = parsed.data;
```

- [ ] **Step 4: 验证启动时校验生效**

临时把 `.env` 的 `DATABASE_URL` 改成非法值,运行 `node -e "require('./src/lib/env')"`(或 `npx tsx src/lib/env.ts`),Expected: 报错并打印字段错误;改回后正常运行。

- [ ] **Step 5: Commit**

```bash
git add .env.example src/lib/env.ts
git commit -m "feat: add zod-validated env singleton"
```

---

## Task 5: decimal.js 封装与 Prisma Decimal 互转

**Files:**
- Create: `src/lib/decimal.ts`

**Interfaces:**
- Consumes: `decimal.js`、`@prisma/client` 的 `Decimal`
- Produces:
  - `D`:配置好的 decimal.js 构造器(scale=2,舍入=四舍五入 `ROUND_HALF_UP`)
  - `toStored(d: D): Prisma.Decimal`
  - `fromStored(d: Prisma.Decimal | string | number): D`
  - `sumAmounts(amounts: D[]): D`
  - 常量 `ZERO: D`

- [ ] **Step 1: 写 decimal.ts**

Create `src/lib/decimal.ts`:
```ts
import { Prisma } from '@prisma/client';
import Decimal from 'decimal.js';

// 全系统金额:18 位精度,2 位小数,四舍五入
Decimal.set({ precision: 18, rounding: Decimal.ROUND_HALF_UP });

export const D = Decimal;
export type D = Decimal;

export const ZERO = new Decimal(0);

/** 领域 Decimal → 数据库存储(Prisma Decimal) */
export function toStored(d: D): Prisma.Decimal {
  return new Prisma.Decimal(d.toFixed(2));
}

/** Prisma Decimal / 字符串 / 数字 → 领域 Decimal */
export function fromStored(d: Prisma.Decimal | string | number): D {
  return new Decimal(typeof d === 'number' ? String(d) : d.toString());
}

/** 金额求和(空数组返回 ZERO) */
export function sumAmounts(amounts: D[]): D {
  return amounts.reduce((acc, a) => acc.plus(a), ZERO);
}

/** 金额比较是否 >= 0(用于校验非负) */
export function isNonNegative(d: D): boolean {
  return d.gte(ZERO);
}
```

- [ ] **Step 2: Commit(此 task 无单测,因纯封装;行为由后续公式库单测间接覆盖)**

```bash
git add src/lib/decimal.ts
git commit -m "feat: add decimal.js money helpers with prisma interop"
```

---

## Task 6: UUID v7 生成(lib/id.ts)

**Files:**
- Create: `src/lib/id.ts`

**Interfaces:**
- Consumes: `uuid` 库
- Produces: `uuidv7(): string`(返回标准 UUID v7 字符串)

- [ ] **Step 1: 安装 uuid**

Run:
```bash
npm install uuid
npm install -D @types/uuid
```

- [ ] **Step 2: 写 lib/id.ts**

Create `src/lib/id.ts`:
```ts
import { v7 as uuidv7base } from 'uuid';

/** 生成时间有序的 UUID v7,作为所有表主键 */
export function uuidv7(): string {
  return uuidv7base();
}
```

- [ ] **Step 3: 验证生成正确**

Run:
```bash
npx tsx -e "import { uuidv7 } from './src/lib/id'; console.log(uuidv7());"
```
Expected: 打印一个 36 字符 UUID,版本位为 `7`(第 3 段首字符 `7`)。

- [ ] **Step 4: Commit**

```bash
git add src/lib/id.ts package.json package-lock.json
git commit -m "feat: add uuid v7 id generator"
```

---

## Task 7: Prisma schema 全量建模(§16)

**Files:**
- Create: `prisma/schema.prisma`
- Create: `src/lib/prisma.ts`(PrismaClient 单例)
- Create: `prisma/migrations/`(由 migrate 生成)

**Interfaces:**
- Consumes: Task 4 的 `DATABASE_URL`、Task 5 的 Decimal 类型、Task 6 的 uuidv7
- Produces: 全量 Prisma 模型,供后续所有 service 使用。模型清单见下方。

- [ ] **Step 1: 初始化 Prisma**

Run:
```bash
npx prisma init --datasource-provider postgresql
```
这会生成 `prisma/schema.prisma` 与 `.env`(若 .env 已存在会提示,保留我们 Task 4 的 .env,只取 schema)。

- [ ] **Step 2: 写全量 schema.prisma**

Create `prisma/schema.prisma`(覆盖默认):
```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 金额统一 Decimal(18,2)
// 主键统一 UUID v7(应用层生成,通过 @default(dbgenerated()) 兜底也可;此处显式传入)

enum UserRole {
  PROJECT_OWNER
  AUTHORIZED_HANDLER
  BUDGET_ADMIN
}

enum MemberRole {
  OWNER
  HANDLER
}

enum ApprovalStatus {
  DRAFT
  PENDING
  APPROVED
  REJECTED
  WITHDRAWN
}

enum BusinessStatus {
  PLACEHOLDER // 登记占位
  CONTRACT    // 合同
  FINANCE_APPROVAL // 财务系统审批
  PAID        // 已支出
}

enum AdjustmentType {
  PROJECT_TOTAL
  ANNUAL
  SUBJECT
  SUBJECT_TRANSFER // 同年度科目调剂
}

enum LineDirection {
  INCREASE
  DECREASE
}

enum LevelType {
  PROJECT
  ANNUAL
  SUBJECT
}

model User {
  id        String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name      String
  role      UserRole
  status    String     @default("active")
  createdAt DateTime   @default(now()) @map("created_at")
  updatedAt DateTime   @updatedAt @map("updated_at")

  memberships       ProjectMember[]
  createdRecords    BusinessRecord[]   @relation("RecordCreator")
  modifiedRecords   BusinessRecord[]   @relation("RecordModifier")
  auditLogs         AuditLog[]
  approvalLogs      ApprovalLog[]
  receipts          ReceiptRecord[]

  @@map("users")
}

model Project {
  id         String   @id @db.Uuid
  code       String   @unique // §16.1 系统内唯一
  name       String
  level      String?
  startDate  DateTime? @map("start_date") @db.Date
  endDate    DateTime? @map("end_date") @db.Date
  ownerId    String   @map("owner_id") @db.Uuid
  remark     String?
  archivedAt DateTime? @map("archived_at")
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")

  owner           User              @relation(fields: [ownerId], references: [id])
  members         ProjectMember[]
  subjects        BudgetSubject[]
  annualBudgets   AnnualBudget[]
  subjectBudgets  SubjectBudget[]
  projectBudget   ProjectBudget?
  applications    InitialBudgetApplication[]
  adjustments     BudgetAdjustment[]
  records         BusinessRecord[]
  receipts        ReceiptRecord[]
  importBatches   ImportBatch[]
  auditLogs       AuditLog[]

  @@map("projects")
}

model ProjectMember {
  id           String    @id @db.Uuid
  projectId    String    @map("project_id") @db.Uuid
  userId       String    @map("user_id") @db.Uuid
  memberRole   MemberRole @map("member_role")
  authorizedAt DateTime  @default(now()) @map("authorized_at")

  project Project @relation(fields: [projectId], references: [id])
  user    User    @relation(fields: [userId], references: [id])

  @@unique([projectId, userId])
  @@map("project_members")
}

model BudgetSubject {
  id          String   @id @db.Uuid
  projectId   String   @map("project_id") @db.Uuid
  parentId    String?  @map("parent_id") @db.Uuid
  code        String   // §16.1 项目内唯一
  name        String
  description String?
  level       Int
  isLeaf      Boolean  @map("is_leaf")
  createdAt   DateTime @default(now()) @map("created_at")

  project        Project         @relation(fields: [projectId], references: [id])
  parent         BudgetSubject?  @relation("SubjectTree", fields: [parentId], references: [id])
  children       BudgetSubject[] @relation("SubjectTree")
  subjectBudgets SubjectBudget[]
  records        BusinessRecord[]

  @@unique([projectId, code]) // §16.1 项目内唯一
  @@index([projectId])
  @@map("budget_subjects")
}

model ProjectBudget {
  projectId         String   @id @map("project_id") @db.Uuid
  initialAmount     Decimal  @map("initial_amount") @db.Decimal(18, 2)
  adjustmentAmount  Decimal  @map("adjustment_amount") @default(0) @db.Decimal(18, 2)
  currentAmount     Decimal  @map("current_amount") @db.Decimal(18, 2)

  project Project @relation(fields: [projectId], references: [id])

  @@map("project_budgets")
}

model AnnualBudget {
  id               String   @id @db.Uuid
  projectId        String   @map("project_id") @db.Uuid
  year             Int
  initialAmount    Decimal  @map("initial_amount") @db.Decimal(18, 2)
  adjustmentAmount Decimal  @map("adjustment_amount") @default(0) @db.Decimal(18, 2)
  currentAmount    Decimal  @map("current_amount") @db.Decimal(18, 2)

  project Project @relation(fields: [projectId], references: [id])

  @@unique([projectId, year]) // §16.1
  @@map("annual_budgets")
}

model SubjectBudget {
  id               String   @id @db.Uuid
  projectId        String   @map("project_id") @db.Uuid
  year             Int
  subjectId        String   @map("subject_id") @db.Uuid
  initialAmount    Decimal  @map("initial_amount") @db.Decimal(18, 2)
  adjustmentAmount Decimal  @map("adjustment_amount") @default(0) @db.Decimal(18, 2)
  currentAmount    Decimal  @map("current_amount") @db.Decimal(18, 2)

  project Project        @relation(fields: [projectId], references: [id])
  subject BudgetSubject  @relation(fields: [subjectId], references: [id])

  @@unique([projectId, year, subjectId]) // §16.1
  @@index([projectId, year])
  @@map("subject_budgets")
}

model InitialBudgetApplication {
  id          String          @id @db.Uuid
  projectId   String          @map("project_id") @db.Uuid
  status      ApprovalStatus  @default(DRAFT)
  applicantId String          @map("applicant_id") @db.Uuid
  submittedAt DateTime?       @map("submitted_at")
  approverId  String?         @map("approver_id") @db.Uuid
  approvedAt  DateTime?       @map("approved_at")
  opinion     String?
  createdAt   DateTime        @default(now()) @map("created_at")
  updatedAt   DateTime        @updatedAt @map("updated_at")

  project   Project @relation(fields: [projectId], references: [id])
  applicant User    @relation("AppInitApplicant", fields: [applicantId], references: [id])

  @@map("initial_budget_applications")
}

model BudgetAdjustment {
  id          String          @id @db.Uuid
  projectId   String          @map("project_id") @db.Uuid
  type        AdjustmentType
  status      ApprovalStatus  @default(DRAFT)
  reason      String?
  applicantId String          @map("applicant_id") @db.Uuid
  approverId  String?         @map("approver_id") @db.Uuid
  createdAt   DateTime        @default(now()) @map("created_at")
  updatedAt   DateTime        @updatedAt @map("updated_at")
  approvedAt  DateTime?       @map("approved_at")

  project   Project @relation(fields: [projectId], references: [id])
  lines     BudgetAdjustmentLine[]
  locks     BudgetLock[]

  @@map("budget_adjustments")
}

model BudgetAdjustmentLine {
  id            String        @id @db.Uuid
  adjustmentId  String        @map("adjustment_id") @db.Uuid
  levelType     LevelType     @map("level_type")
  year          Int?
  subjectId     String?       @map("subject_id") @db.Uuid
  direction     LineDirection
  amount        Decimal       @db.Decimal(18, 2)

  adjustment BudgetAdjustment @relation(fields: [adjustmentId], references: [id])

  @@map("budget_adjustment_lines")
}

model BudgetLock {
  id            String    @id @db.Uuid
  adjustmentId  String    @map("adjustment_id") @db.Uuid
  projectId     String    @map("project_id") @db.Uuid
  year          Int?
  subjectId     String    @map("subject_id") @db.Uuid
  amount        Decimal   @db.Decimal(18, 2)
  createdAt     DateTime  @default(now()) @map("created_at")
  releasedAt    DateTime? @map("released_at")

  adjustment BudgetAdjustment @relation(fields: [adjustmentId], references: [id])

  @@index([subjectId, releasedAt])
  @@map("budget_locks")
}

model SubjectChangeApplication {
  id              String         @id @db.Uuid
  projectId       String         @map("project_id") @db.Uuid
  status          ApprovalStatus @default(DRAFT)
  beforeSnapshot  Json           @map("before_snapshot")
  afterSnapshot   Json           @map("after_snapshot")
  applicantId     String         @map("applicant_id") @db.Uuid
  approverId      String?        @map("approver_id") @db.Uuid
  createdAt       DateTime       @default(now()) @map("created_at")
  updatedAt       DateTime       @updatedAt @map("updated_at")

  @@map("subject_change_applications")
}

model BusinessRecord {
  id            String          @id @db.Uuid
  projectId     String          @map("project_id") @db.Uuid
  budgetYear    Int             @map("budget_year")
  subjectId     String          @map("subject_id") @db.Uuid
  amount        Decimal         @db.Decimal(18, 2)
  businessDate  DateTime        @map("business_date") @db.Date
  enteredAt     DateTime        @default(now()) @map("entered_at")
  handler       String
  summary       String
  status        BusinessStatus
  remark        String?
  isVoid        Boolean         @default(false) @map("is_void")
  voidReason    String?         @map("void_reason")
  voidedBy      String?         @map("voided_by") @db.Uuid
  voidedAt      DateTime?       @map("voided_at")
  createdById   String          @map("created_by") @db.Uuid
  modifiedById  String?         @map("modified_by") @db.Uuid
  createdAt     DateTime        @default(now()) @map("created_at")
  updatedAt     DateTime        @updatedAt @map("updated_at")

  project    Project         @relation(fields: [projectId], references: [id])
  subject    BudgetSubject   @relation(fields: [subjectId], references: [id])
  createdBy  User            @relation("RecordCreator", fields: [createdById], references: [id])
  modifiedBy User?           @relation("RecordModifier", fields: [modifiedById], references: [id])
  history    BusinessRecordHistory[]

  @@index([projectId, budgetYear, subjectId, isVoid])
  @@index([projectId, budgetYear])
  @@map("business_records")
}

model BusinessRecordHistory {
  id             String   @id @db.Uuid
  businessRecordId String @map("business_record_id") @db.Uuid
  action         String
  beforeData     Json?    @map("before_data")
  afterData      Json?    @map("after_data")
  operatorId     String   @map("operator_id") @db.Uuid
  operatedAt     DateTime @default(now()) @map("operated_at")
  reason         String?

  businessRecord BusinessRecord @relation(fields: [businessRecordId], references: [id])

  @@map("business_record_history")
}

model ReceiptRecord {
  id          String   @id @db.Uuid
  projectId   String   @map("project_id") @db.Uuid
  receiptDate DateTime @map("receipt_date") @db.Date
  amount      Decimal  @db.Decimal(18, 2)
  summary     String?
  remark      String?
  creatorId   String   @map("creator_id") @db.Uuid
  createdAt   DateTime @default(now()) @map("created_at")

  project Project @relation(fields: [projectId], references: [id])
  creator User    @relation(fields: [creatorId], references: [id])

  @@map("receipt_records")
}

model ImportBatch {
  id              String   @id @db.Uuid
  projectId       String   @map("project_id") @db.Uuid
  fileName        String   @map("file_name")
  templateVersion String   @map("template_version")
  status          String   @default("pending")
  creatorId       String   @map("creator_id") @db.Uuid
  createdAt       DateTime @default(now()) @map("created_at")
  confirmedAt     DateTime? @map("confirmed_at")

  project Project   @relation(fields: [projectId], references: [id])
  rows    ImportRow[]

  @@map("import_batches")
}

model ImportRow {
  id               String  @id @db.Uuid
  batchId          String  @map("batch_id") @db.Uuid
  rowNo            Int     @map("row_no")
  parsedData       Json    @map("parsed_data")
  validationStatus String  @map("validation_status")
  errors           Json?
  duplicateFlag    Boolean @default(false) @map("duplicate_flag")
  forcedImport     Boolean @default(false) @map("forced_import")

  batch ImportBatch @relation(fields: [batchId], references: [id])

  @@map("import_rows")
}

model ApprovalLog {
  id         String   @id @db.Uuid
  objectType String   @map("object_type")
  objectId   String   @map("object_id") @db.Uuid
  action     String
  operatorId String   @map("operator_id") @db.Uuid
  opinion    String?
  operatedAt DateTime @default(now()) @map("operated_at")

  operator User @relation(fields: [operatorId], references: [id])

  @@index([objectType, objectId])
  @@map("approval_logs")
}

model AuditLog {
  id          String   @id @db.Uuid
  projectId   String?  @map("project_id") @db.Uuid
  objectType  String   @map("object_type")
  objectId    String   @map("object_id")
  action      String
  beforeData  Json?    @map("before_data")
  afterData   Json?    @map("after_data")
  operatorId  String   @map("operator_id") @db.Uuid
  operatedAt  DateTime @default(now()) @map("operated_at")

  project Project? @relation(fields: [projectId], references: [id])
  operator User    @relation(fields: [operatorId], references: [id])

  @@index([objectType, objectId])
  @@index([projectId])
  @@map("audit_logs")
}
```

- [ ] **Step 3: 写 PrismaClient 单例**

Create `src/lib/prisma.ts`:
```ts
import { PrismaClient } from '@prisma/client';
import { env } from '@/lib/env';

const globalForPrisma = globalThis as unknown as { prisma?: PrismaClient };

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: env.NODE_ENV === 'development' ? ['query', 'error', 'warn'] : ['error'],
  });

if (env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;
```

- [ ] **Step 4: 生成 client 并创建首个 migration**

Run:
```bash
docker compose up -d postgres
npx prisma migrate dev --name init
npx prisma generate
```
Expected: 生成 `prisma/migrations/<timestamp>_init/` 并建表成功。

- [ ] **Step 5: 验证表已建**

Run:
```bash
npx prisma db pull --print | head -20
```
Expected: 能拉到表结构(说明迁移成功)。

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: add full prisma schema for v1 data model"
```

---

## Task 8: 金额纯公式库 — money.ts 与 occupancy.ts(TDD)

**Files:**
- Create: `src/lib/budget/money.ts`
- Create: `src/lib/budget/occupancy.ts`
- Test: `tests/lib/budget/occupancy.test.ts`

**Interfaces:**
- Consumes: `src/lib/decimal.ts` 的 `D`、`fromStored`、`sumAmounts`、`ZERO`
- Produces:
  - `money.ts`:`formatMoney(d: D): string`(两位小数,千分位)、`isNegative(d: D): boolean`、`parseMoney(s: string): D`
  - `occupancy.ts`:`computeOccupancy(args: { records: { amount: D; status: BusinessStatus; isVoid: boolean }[] })` 返回 `{ paid: D; payable: D; totalOccupied: D }`

- [ ] **Step 1: 写 money.ts(无单测,纯格式化)**

Create `src/lib/budget/money.ts`:
```ts
import Decimal from 'decimal.js';
import { D } from '@/lib/decimal';

const nf = new Intl.NumberFormat('zh-CN', {
  minimumFractionDigits: 2,
  maximumFractionDigits: 2,
});

/** 金额 → 展示字符串(两位小数,千分位),用于前端展示 */
export function formatMoney(d: D): string {
  return nf.format(Number(d.toFixed(2)));
}

export function isNegative(d: D): boolean {
  return d.lt(0);
}

/** 字符串(来自接口)→ Decimal */
export function parseMoney(s: string): D {
  return new Decimal(s);
}
```

- [ ] **Step 2: 写 occupancy.ts 的失败测试**

Create `tests/lib/budget/occupancy.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { computeOccupancy } from '@/lib/budget/occupancy';
import { fromStored } from '@/lib/decimal';

const amt = (n: string) => fromStored(n);

describe('computeOccupancy', () => {
  it('已支出计入 paid 与 totalOccupied,不计入 payable', () => {
    const r = computeOccupancy({
      records: [{ amount: amt('100'), status: 'PAID', isVoid: false }],
    });
    expect(r.paid.toString()).toBe('100.00');
    expect(r.payable.toString()).toBe('0.00');
    expect(r.totalOccupied.toString()).toBe('100.00');
  });

  it('非已支出三类计入 payable 与 totalOccupied,不计入 paid', () => {
    const r = computeOccupancy({
      records: [
        { amount: amt('50'), status: 'PLACEHOLDER', isVoid: false },
        { amount: amt('30'), status: 'CONTRACT', isVoid: false },
        { amount: amt('20'), status: 'FINANCE_APPROVAL', isVoid: false },
      ],
    });
    expect(r.paid.toString()).toBe('0.00');
    expect(r.payable.toString()).toBe('100.00');
    expect(r.totalOccupied.toString()).toBe('100.00');
  });

  it('作废记录不计入任何占用(§8.6)', () => {
    const r = computeOccupancy({
      records: [
        { amount: amt('100'), status: 'PAID', isVoid: false },
        { amount: amt('999'), status: 'PAID', isVoid: true },
      ],
    });
    expect(r.totalOccupied.toString()).toBe('100.00');
  });

  it('混合多种状态正确分类汇总', () => {
    const r = computeOccupancy({
      records: [
        { amount: amt('100'), status: 'PAID', isVoid: false },
        { amount: amt('50'), status: 'CONTRACT', isVoid: false },
        { amount: amt('25.5'), status: 'PLACEHOLDER', isVoid: false },
        { amount: amt('10'), status: 'PAID', isVoid: true },
      ],
    });
    expect(r.paid.toString()).toBe('100.00');
    expect(r.payable.toString()).toBe('75.50');
    expect(r.totalOccupied.toString()).toBe('175.50');
  });
});
```

- [ ] **Step 3: 运行测试,确认失败**

Run:
```bash
npx vitest run tests/lib/budget/occupancy.test.ts
```
Expected: FAIL(`computeOccupancy` 未定义 / 模块不存在)。

- [ ] **Step 4: 写 occupancy.ts 实现**

Create `src/lib/budget/occupancy.ts`:
```ts
import { BusinessStatus } from '@prisma/client';
import { D, fromStored, sumAmounts } from '@/lib/decimal';

export interface OccupancyInput {
  records: { amount: D | { toString(): string }; status: BusinessStatus; isVoid: boolean }[];
}

export interface OccupancyResult {
  paid: D;
  payable: D;
  totalOccupied: D;
}

/**
 * §4.5 预算占用计算
 * 已支出金额 = 状态为 PAID 的有效记录金额合计
 * 应付未付 = 状态为 PLACEHOLDER/CONTRACT/FINANCE_APPROVAL 的有效记录金额合计
 * 总占用 = 已支出 + 应付未付
 * 作废记录不计入。
 */
export function computeOccupancy(input: OccupancyInput): OccupancyResult {
  const effective = input.records.filter((r) => !r.isVoid);
  const paid = sumAmounts(
    effective.filter((r) => r.status === 'PAID').map((r) => fromStored(r.amount.toString())),
  );
  const payable = sumAmounts(
    effective
      .filter((r) => r.status !== 'PAID')
      .map((r) => fromStored(r.amount.toString())),
  );
  const totalOccupied = paid.plus(payable);
  return { paid, payable, totalOccupied };
}
```

- [ ] **Step 5: 运行测试,确认通过**

Run:
```bash
npx vitest run tests/lib/budget/occupancy.test.ts
```
Expected: PASS(4 个用例全绿)。

- [ ] **Step 6: Commit**

```bash
git add src/lib/budget/money.ts src/lib/budget/occupancy.ts tests/lib/budget/occupancy.test.ts
git commit -m "feat: add budget occupancy computation with tests"
```

---

## Task 9: 执行率纯公式 — rate.ts(TDD,含除零)

**Files:**
- Create: `src/lib/budget/rate.ts`
- Test: `tests/lib/budget/rate.test.ts`

**Interfaces:**
- Consumes: `occupancy.ts` 的 `totalOccupied`、`lib/decimal.ts`
- Produces: `executionRate(totalOccupied: D, currentBudget: D): number | null`(当前预算为 0 返回 `null`,表示"—";否则返回 0-1 区间比例,前端自行格式化为百分比)

- [ ] **Step 1: 写失败测试**

Create `tests/lib/budget/rate.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { executionRate } from '@/lib/budget/rate';
import { fromStored } from '@/lib/decimal';

describe('executionRate', () => {
  it('正常计算执行率(占用/当前预算)', () => {
    expect(executionRate(fromStored('80'), fromStored('100'))).toBeCloseTo(0.8, 10);
  });

  it('当前预算为 0 时返回 null(§4.5 不得除零)', () => {
    expect(executionRate(fromStored('50'), fromStored('0'))).toBeNull();
  });

  it('超预算时执行率 > 1', () => {
    expect(executionRate(fromStored('120'), fromStored('100'))).toBeCloseTo(1.2, 10);
  });

  it('占用为 0 时执行率为 0', () => {
    expect(executionRate(fromStored('0'), fromStored('100'))).toBeCloseTo(0, 10);
  });
});
```

- [ ] **Step 2: 运行测试,确认失败**

Run:
```bash
npx vitest run tests/lib/budget/rate.test.ts
```
Expected: FAIL(模块不存在)。

- [ ] **Step 3: 写实现**

Create `src/lib/budget/rate.ts`:
```ts
import { D } from '@/lib/decimal';

/**
 * §4.5 执行率 = 总占用 ÷ 当前预算
 * 当前预算为 0 时返回 null(界面显示"—",不产生除零错误)
 * 返回值为 0-1 区间的 number(可能 > 1 表示超预算),用于百分比展示。
 */
export function executionRate(totalOccupied: D, currentBudget: D): number | null {
  if (currentBudget.isZero()) return null;
  return Number(totalOccupied.div(currentBudget));
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run:
```bash
npx vitest run tests/lib/budget/rate.test.ts
```
Expected: PASS(4 个用例全绿)。

- [ ] **Step 5: Commit**

```bash
git add src/lib/budget/rate.ts tests/lib/budget/rate.test.ts
git commit -m "feat: add execution rate with divide-by-zero handling"
```

---

## Task 10: 可调额度纯公式 — adjustable.ts(TDD)

**Files:**
- Create: `src/lib/budget/adjustable.ts`
- Test: `tests/lib/budget/adjustable.test.ts`

**Interfaces:**
- Consumes: `lib/decimal.ts`、`occupancy.ts`
- Produces:
  - `adjustableAmount(currentBudget: D, totalOccupied: D): D` — §7.4 科目可调额度
  - `operableAmount(currentBudget: D, totalOccupied: D, pendingLock: D): D` — §7.4 可操作额度

- [ ] **Step 1: 写失败测试**

Create `tests/lib/budget/adjustable.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { adjustableAmount, operableAmount } from '@/lib/budget/adjustable';
import { fromStored } from '@/lib/decimal';

describe('adjustableAmount (§7.4 可调额度)', () => {
  it('可调额度 = 当前预算 - 总占用', () => {
    expect(adjustableAmount(fromStored('100'), fromStored('30')).toString()).toBe('70.00');
  });

  it('占用等于预算时可调额度为 0', () => {
    expect(adjustableAmount(fromStored('100'), fromStored('100')).toString()).toBe('0.00');
  });

  it('超占用时可调额度为负(允许)', () => {
    expect(adjustableAmount(fromStored('100'), fromStored('130')).toString()).toBe('-30.00');
  });
});

describe('operableAmount (§7.4 可操作额度)', () => {
  it('可操作额度 = 当前预算 - 总占用 - 待审批锁定', () => {
    expect(
      operableAmount(fromStored('100'), fromStored('30'), fromStored('20')).toString(),
    ).toBe('50.00');
  });

  it('锁定使可操作额度为负时返回负值(预警用)', () => {
    expect(
      operableAmount(fromStored('100'), fromStored('80'), fromStored('40')).toString(),
    ).toBe('-20.00');
  });

  it('无锁定时等于可调额度', () => {
    expect(operableAmount(fromStored('100'), fromStored('30'), fromStored('0')).toString()).toBe(
      '70.00',
    );
  });
});
```

- [ ] **Step 2: 运行测试,确认失败**

Run:
```bash
npx vitest run tests/lib/budget/adjustable.test.ts
```
Expected: FAIL。

- [ ] **Step 3: 写实现**

Create `src/lib/budget/adjustable.ts`:
```ts
import { D } from '@/lib/decimal';

/**
 * §7.4 科目可调额度 = 科目当前预算 - 科目总占用
 */
export function adjustableAmount(currentBudget: D, totalOccupied: D): D {
  return currentBudget.minus(totalOccupied);
}

/**
 * §7.4 可操作额度 = 科目当前预算 - 科目总占用 - 待审批调整锁定金额
 * 返回负值表示锁定/占用已超出,用于预警(§13.2)。
 */
export function operableAmount(currentBudget: D, totalOccupied: D, pendingLock: D): D {
  return currentBudget.minus(totalOccupied).minus(pendingLock);
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run:
```bash
npx vitest run tests/lib/budget/adjustable.test.ts
```
Expected: PASS(6 个用例全绿)。

- [ ] **Step 5: Commit**

```bash
git add src/lib/budget/adjustable.ts tests/lib/budget/adjustable.test.ts
git commit -m "feat: add adjustable and operable amount formulas"
```

---

## Task 11: 父节点上卷纯公式 — rollup.ts(TDD)

**Files:**
- Create: `src/lib/budget/rollup.ts`
- Test: `tests/lib/budget/rollup.test.ts`

**Interfaces:**
- Consumes: `lib/decimal.ts`
- Produces:
  - `rollupSubjectBudgets(leafCurrents: { subjectId: string; current: D }[]): D` — 非叶节点当前预算 = 叶节点之和(§4.4)
  - `buildTreeRollup(nodes)` — 把扁平科目列表按 parent 构建树并自底向上汇总金额

- [ ] **Step 1: 写失败测试**

Create `tests/lib/budget/rollup.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { rollupSubjectBudgets, buildTreeRollup } from '@/lib/budget/rollup';
import { fromStored } from '@/lib/decimal';

describe('rollupSubjectBudgets', () => {
  it('非叶节点当前预算 = 叶节点当前预算之和', () => {
    const r = rollupSubjectBudgets([
      { subjectId: 'leaf1', current: fromStored('100') },
      { subjectId: 'leaf2', current: fromStored('50.5') },
    ]);
    expect(r.toString()).toBe('150.50');
  });

  it('空列表返回 0', () => {
    expect(rollupSubjectBudgets([]).toString()).toBe('0.00');
  });
});

describe('buildTreeRollup', () => {
  it('按 parent 关系自底向上汇总父节点金额', () => {
    const nodes = [
      { id: 'A', parentId: null, isLeaf: false, current: fromStored('0') },
      { id: 'A1', parentId: 'A', isLeaf: false, current: fromStored('0') },
      { id: 'A1a', parentId: 'A1', isLeaf: true, current: fromStored('100') },
      { id: 'A1b', parentId: 'A1', isLeaf: true, current: fromStored('20') },
      { id: 'A2', parentId: 'A', isLeaf: true, current: fromStored('30') },
    ];
    const rolled = buildTreeRollup(nodes);
    const find = (id: string) => rolled.find((n) => n.id === id)!;
    expect(find('A1').current.toString()).toBe('120.00');
    expect(find('A').current.toString()).toBe('150.00');
  });
});
```

- [ ] **Step 2: 运行测试,确认失败**

Run:
```bash
npx vitest run tests/lib/budget/rollup.test.ts
```
Expected: FAIL。

- [ ] **Step 3: 写实现**

Create `src/lib/budget/rollup.ts`:
```ts
import { D, ZERO, sumAmounts } from '@/lib/decimal';

export interface SubjectNode {
  id: string;
  parentId: string | null;
  isLeaf: boolean;
  current: D;
}

/**
 * §4.4 非叶节点当前预算 = 其下全部叶节点当前预算之和
 */
export function rollupSubjectBudgets(leafCurrents: { subjectId: string; current: D }[]): D {
  return sumAmounts(leafCurrents.map((l) => l.current));
}

/**
 * 把扁平科目列表构建成树,并自底向上汇总每个非叶节点的 current。
 * 返回的数组每个节点 current 已被更新为(若是叶节点则原值;若是非叶节点则子树叶节点之和)。
 */
export function buildTreeRollup(nodes: SubjectNode[]): SubjectNode[] {
  const byId = new Map(nodes.map((n) => [n.id, { ...n }]));

  // 递归计算某节点子树叶节点之和
  const compute = (nodeId: string): D => {
    const node = byId.get(nodeId)!;
    if (node.isLeaf) return node.current;
    const children = nodes.filter((n) => n.parentId === nodeId);
    if (children.length === 0) return ZERO;
    const childSum = sumAmounts(children.map((c) => compute(c.id)));
    node.current = childSum;
    return childSum;
  };

  // 对所有根节点(parentId == null)递归,会顺带填好所有非叶节点
  for (const n of byId.values()) {
    if (n.parentId === null) compute(n.id);
  }

  return Array.from(byId.values());
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run:
```bash
npx vitest run tests/lib/budget/rollup.test.ts
```
Expected: PASS(3 个用例全绿)。

- [ ] **Step 5: Commit**

```bash
git add src/lib/budget/rollup.ts tests/lib/budget/rollup.test.ts
git commit -m "feat: add subject tree rollup formula"
```

---

## Task 12: 公式库统一导出 + 全量回归

**Files:**
- Create: `src/lib/budget/index.ts`

**Interfaces:**
- Consumes: Task 8-11 的公式模块
- Produces: 统一入口 `@/lib/budget`

- [ ] **Step 1: 写 index.ts**

Create `src/lib/budget/index.ts`:
```ts
export * from './money';
export * from './occupancy';
export * from './rate';
export * from './adjustable';
export * from './rollup';
```

- [ ] **Step 2: 跑全量公式库测试**

Run:
```bash
npm test -- tests/lib/budget
```
Expected: occupancy/rate/adjustable/rollup 全部 PASS。

- [ ] **Step 3: Commit**

```bash
git add src/lib/budget/index.ts
git commit -m "feat: export budget formula library barrel"
```

---

## Task 13: mock 鉴权会话(lib/auth/session.ts)

**Files:**
- Create: `src/lib/auth/session.ts`

**Interfaces:**
- Consumes: `lib/env.ts`(MOCK_AUTH)、`lib/prisma.ts`
- Produces: `getCurrentUser(req): Promise<User>` — 从 mock header `x-mock-user-id` 读用户;`requireUser(req)` — 取不到抛 401。

- [ ] **Step 1: 写 session.ts**

Create `src/lib/auth/session.ts`:
```ts
import { User } from '@prisma/client';
import { headers } from 'next/headers';
import { prisma } from '@/lib/prisma';
import { env } from '@/lib/env';

/**
 * 获取当前用户。V1 mock:从 x-mock-user-id header 读取。
 * 未来切换真鉴权时,只改此函数的"用户来源",业务代码不变。
 */
export async function getCurrentUser(): Promise<User | null> {
  if (!env.MOCK_AUTH) {
    // 占位:真鉴权接入点(如 NextAuth getSession)。V1 暂不实现。
    throw new Error('MOCK_AUTH=false 时需接入真实鉴权,当前未实现');
  }
  const h = headers();
  const mockUserId = h.get('x-mock-user-id');
  if (!mockUserId) return null;
  return prisma.user.findUnique({ where: { id: mockUserId } });
}

export async function requireUser(): Promise<User> {
  const user = await getCurrentUser();
  if (!user) throw new HTTPError(401, '未登录或用户不存在');
  return user;
}

export class HTTPError extends Error {
  constructor(public status: number, message: string) {
    super(message);
  }
}
```

- [ ] **Step 2: Commit(行为由 Task 15 权限矩阵单测间接覆盖)**

```bash
git add src/lib/auth/session.ts
git commit -m "feat: add mock auth session with real-auth switchover point"
```

---

## Task 14: RBAC 守卫(permissions.ts + projects.ts)

**Files:**
- Create: `src/lib/auth/permissions.ts`
- Create: `src/lib/auth/projects.ts`
- Test: `tests/lib/auth/permissions.test.ts`

**Interfaces:**
- Consumes: `lib/auth/session.ts`、`lib/prisma.ts`、PRD §2.2 矩阵
- Produces:
  - `permissions.ts`:`can(user, action, ctx?): boolean`、`requirePermission(user, action, projectId?): Promise<void>`
  - `projects.ts`:`getAccessibleProjectIds(user): Promise<string[]>`(预算管理员返回全部;否则返回 owner + 成员项目)
  - 动作枚举 `Action`(覆盖 §2.2 矩阵列)

- [ ] **Step 1: 定义 Action 枚举与权限矩阵**

Create `src/lib/auth/permissions.ts`:
```ts
import { UserRole } from '@prisma/client';
import { getAccessibleProjectIds } from './projects';
import { HTTPError } from '@/lib/auth/session';

// §2.2 权限矩阵的动作
export type Action =
  | 'project:view' // 查看获授权项目
  | 'project:viewAll' // 查看全部项目(仅管理员)
  | 'budget:editInitial' // 编制初始预算
  | 'budget:editSubjectTree' // 维护初始科目树
  | 'budget:adjust' // 发起预算调整
  | 'budget:changeSubject' // 发起科目变更
  | 'budget:approve' // 审批(编制/调整/科目变更)
  | 'record:create' // 新增业务记录
  | 'record:edit' // 修改业务记录
  | 'record:void' // 作废业务记录
  | 'record:import' // Excel 导入
  | 'audit:view'; // 查看操作审计

const MATRIX: Record<UserRole, Set<Action>> = {
  PROJECT_OWNER: new Set<Action>([
    'project:view',
    'budget:editInitial',
    'budget:editSubjectTree',
    'budget:adjust',
    'budget:changeSubject',
    'record:create',
    'record:edit',
    'record:void',
    'record:import',
    'audit:view',
  ]),
  AUTHORIZED_HANDLER: new Set<Action>([
    'project:view',
    'budget:editInitial',
    'budget:editSubjectTree',
    'budget:adjust',
    'budget:changeSubject',
    'record:create',
    'record:edit',
    'record:void',
    'record:import',
    'audit:view',
  ]),
  BUDGET_ADMIN: new Set<Action>([
    'project:view',
    'project:viewAll',
    'budget:editInitial',
    'budget:editSubjectTree',
    'budget:adjust',
    'budget:changeSubject',
    'budget:approve',
    'record:create',
    'record:edit',
    'record:void',
    'record:import',
    'audit:view',
  ]),
};

/** 是否有某动作权限(仅按角色,不含项目范围) */
export function can(user: { role: UserRole }, action: Action): boolean {
  return MATRIX[user.role]?.has(action) ?? false;
}

/**
 * 要求用户具备某动作权限;若涉及具体项目,额外校验项目访问范围。
 * 服务端权限再校验(§15.3)。
 */
export async function requirePermission(
  user: { id: string; role: UserRole },
  action: Action,
  projectId?: string,
): Promise<void> {
  if (!can(user, action)) {
    throw new HTTPError(403, `无权限执行操作:${action}`);
  }
  if (projectId && action !== 'project:viewAll') {
    const accessible = await getAccessibleProjectIds(user);
    if (!accessible.includes(projectId)) {
      throw new HTTPError(403, '无权访问该项目');
    }
  }
}
```

- [ ] **Step 2: 写 projects.ts**

Create `src/lib/auth/projects.ts`:
```ts
import { UserRole } from '@prisma/client';
import { prisma } from '@/lib/prisma';

/**
 * 返回用户可访问的项目 ID 列表。
 * 预算管理员返回全部项目;否则返回其作为 owner 或成员(经办人)的项目。
 */
export async function getAccessibleProjectIds(user: {
  id: string;
  role: UserRole;
}): Promise<string[]> {
  if (user.role === 'BUDGET_ADMIN') {
    const all = await prisma.project.findMany({ select: { id: true } });
    return all.map((p) => p.id);
  }
  const owned = await prisma.project.findMany({
    where: { ownerId: user.id },
    select: { id: true },
  });
  const member = await prisma.projectMember.findMany({
    where: { userId: user.id },
    select: { projectId: true },
  });
  const set = new Set<string>([...owned.map((p) => p.id), ...member.map((m) => m.projectId)]);
  return Array.from(set);
}
```

- [ ] **Step 3: 写权限矩阵单测**

Create `tests/lib/auth/permissions.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { can, Action } from '@/lib/auth/permissions';
import { UserRole } from '@prisma/client';

describe('RBAC matrix (§2.2)', () => {
  const owner = { role: UserRole.PROJECT_OWNER } as const;
  const handler = { role: UserRole.AUTHORIZED_HANDLER } as const;
  const admin = { role: UserRole.BUDGET_ADMIN } as const;

  it('项目负责人可编制/调整/记业务,不可审批', () => {
    expect(can(owner, 'budget:editInitial')).toBe(true);
    expect(can(owner, 'budget:adjust')).toBe(true);
    expect(can(owner, 'record:create')).toBe(true);
    expect(can(owner, 'budget:approve')).toBe(false);
  });

  it('授权经办人与项目负责人权限相同', () => {
    for (const a of [
      'budget:editInitial',
      'budget:adjust',
      'record:void',
      'record:import',
    ] as Action[]) {
      expect(can(handler, a)).toBe(can(owner, a));
    }
  });

  it('预算管理员可审批,且可查看全部项目', () => {
    expect(can(admin, 'budget:approve')).toBe(true);
    expect(can(admin, 'project:viewAll')).toBe(true);
    expect(can(owner, 'project:viewAll')).toBe(false);
  });

  it('所有角色都不能执行未定义动作(can 返回 false)', () => {
    expect(can(owner, 'budget:approve' as Action)).toBe(false);
  });
});
```

- [ ] **Step 4: 运行测试,确认通过**

Run:
```bash
npx vitest run tests/lib/auth/permissions.test.ts
```
Expected: PASS(4 个用例全绿)。注:此测试只测纯函数 `can`(无 DB 依赖);`requirePermission` 的项目范围校验在集成测里覆盖。

- [ ] **Step 5: Commit**

```bash
git add src/lib/auth/permissions.ts src/lib/auth/projects.ts tests/lib/auth/permissions.test.ts
git commit -m "feat: add RBAC guard with permission matrix"
```

---

## Task 15: 统一审计中间件(decorator + interceptor + snapshot)

**Files:**
- Create: `src/server/audit/snapshot.ts`
- Create: `src/server/audit/interceptor.ts`
- Test: `tests/server/audit/snapshot.test.ts`

**Interfaces:**
- Consumes: `lib/prisma.ts`、`lib/auth/session.ts`
- Produces:
  - `snapshot.ts`:`snapshotRow(record): object`(序列化整行为可存 JSONB 的对象,Decimal 转字符串)
  - `interceptor.ts`:`recordAudit(args)` — 在业务事务内写 `audit_logs`(整行 before/after)

- [ ] **Step 1: 写 snapshot.ts 的失败测试**

Create `tests/server/audit/snapshot.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { snapshotRow } from '@/server/audit/snapshot';
import { Prisma } from '@prisma/client';

describe('snapshotRow', () => {
  it('Decimal 字段转为字符串(便于 JSONB 存储)', () => {
    const row = { id: 'x', amount: new Prisma.Decimal('100.50'), name: 'foo' };
    const snap = snapshotRow(row);
    expect(snap.amount).toBe('100.50');
    expect(snap.name).toBe('foo');
  });

  it('null 与 Date 保留原样(Date 转 ISO 字符串)', () => {
    const d = new Date('2026-01-01T00:00:00Z');
    const snap = snapshotRow({ id: 'x', date: d, nullable: null });
    expect(snap.nullable).toBeNull();
    expect(snap.date).toBe(d.toISOString());
  });
});
```

- [ ] **Step 2: 运行测试,确认失败**

Run:
```bash
npx vitest run tests/server/audit/snapshot.test.ts
```
Expected: FAIL。

- [ ] **Step 3: 写 snapshot.ts**

Create `src/server/audit/snapshot.ts`:
```ts
import { Prisma } from '@prisma/client';

/**
 * 把一行记录序列化为可存 JSONB 的快照对象(§14.1 before/after 整行快照)。
 * Prisma.Decimal → 字符串;Date → ISO 字符串;其余原样。
 */
export function snapshotRow(row: Record<string, unknown>): Record<string, unknown> {
  const out: Record<string, unknown> = {};
  for (const [k, v] of Object.entries(row)) {
    if (v instanceof Prisma.Decimal) {
      out[k] = v.toString();
    } else if (v instanceof Date) {
      out[k] = v.toISOString();
    } else {
      out[k] = v;
    }
  }
  return out;
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run:
```bash
npx vitest run tests/server/audit/snapshot.test.ts
```
Expected: PASS。

- [ ] **Step 5: 写 interceptor.ts(事务内写审计)**

Create `src/server/audit/interceptor.ts`:
```ts
import { Prisma, PrismaClient } from '@prisma/client';
import { snapshotRow } from './snapshot';

export interface AuditContext {
  projectId?: string;
  objectType: string;
  objectId: string;
  action: string;
  operatorId: string;
  before?: Record<string, unknown> | null;
  after?: Record<string, unknown> | null;
}

/**
 * 在业务事务内写一条审计日志(§14.1)。
 * 调用方传入 tx(来自 prisma.$transaction),保证日志与业务同事务、审计链不断(§14.2)。
 */
export function recordAudit(
  tx: Prisma.TransactionClient | PrismaClient,
  ctx: AuditContext,
) {
  return tx.auditLog.create({
    data: {
      projectId: ctx.projectId ?? null,
      objectType: ctx.objectType,
      objectId: ctx.objectId,
      action: ctx.action,
      beforeData: (ctx.before ? snapshotRow(ctx.before) : null) as Prisma.InputJsonValue,
      afterData: (ctx.after ? snapshotRow(ctx.after) : null) as Prisma.InputJsonValue,
      operatorId: ctx.operatorId,
    },
  });
}
```

- [ ] **Step 6: Commit(事务集成行为在业务 service 集成测中验证)**

```bash
git add src/server/audit/snapshot.ts src/server/audit/interceptor.ts tests/server/audit/snapshot.test.ts
git commit -m "feat: add audit snapshot and in-transaction audit interceptor"
```

---

## Task 16: 根布局主题(AntD ConfigProvider 紫色)

**Files:**
- Modify: `src/app/layout.tsx`
- Create: `src/components/ui/MoneyText.tsx`(业务封装组件雏形)

**Interfaces:**
- Consumes: Task 1 的 layout
- Produces: 全局紫色主题;`<MoneyText>` 金额展示组件(右对齐、两位小数、负数风险色)。

- [ ] **Step 1: 改 layout.tsx 注入 ConfigProvider**

Replace `src/app/layout.tsx` 内容:
```tsx
import type { Metadata } from 'next';
import { ConfigProvider } from 'antd';
import zhCN from 'antd/locale/zh_CN';
import 'antd/dist/reset.css';
import './globals.css';

export const metadata: Metadata = {
  title: '预算管理系统',
  description: '科研项目预算管理系统',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh-CN">
      <body>
        <ConfigProvider
          theme={{
            token: {
              colorPrimary: '#7c3aed', // 对齐原型紫色主题
              borderRadius: 4,
            },
          }}
          locale={zhCN}
        >
          {children}
        </ConfigProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 2: 写 MoneyText 组件**

Create `src/components/ui/MoneyText.tsx`:
```tsx
'use client';
import { Typography } from 'antd';
import { formatMoney, isNegative } from '@/lib/budget/money';
import { parseMoney } from '@/lib/budget/money';

const { Text } = Typography;

interface Props {
  /** 金额字符串(来自接口,§5 字符串传输) */
  value: string;
  /** 负数是否显示"超预算"风险色(§12.2) */
  riskOnNegative?: boolean;
}

/** §12.2 金额统一右对齐、两位小数;负数用风险色 */
export function MoneyText({ value, riskOnNegative = true }: Props) {
  const d = parseMoney(value);
  const text = formatMoney(d);
  const isRisk = riskOnNegative && isNegative(d);
  return (
    <Text style={{ float: 'right', color: isRisk ? '#cf1322' : undefined }}>
      {isRisk ? `${text} 超预算` : text}
    </Text>
  );
}
```

- [ ] **Step 3: 验证构建与主题**

Run:
```bash
npm run build
```
Expected: 构建成功。

- [ ] **Step 4: Commit**

```bash
git add src/app/layout.tsx src/components/ui/MoneyText.tsx
git commit -m "feat: add antd purple theme and moneytext component"
```

---

## Task 17: 地基层验收与集成测冒烟

**Files:**
- Test: `tests/server/foundation.smoke.test.ts`

**Interfaces:**
- Consumes: 全部地基层产物
- Produces: 一个集成测,验证 Prisma 连接 + 公式库 + 审计写入协同工作(冒烟)。

- [ ] **Step 1: 配置 Vitest 的 setup(确保 prisma client 可连测试库)**

Create `vitest.config.ts`(若 create-next-app 未生成):
```ts
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  test: {
    environment: 'node',
    globals: true,
  },
});
```

- [ ] **Step 2: 写冒烟集成测**

Create `tests/server/foundation.smoke.test.ts`:
```ts
import { describe, it, expect, beforeAll } from 'vitest';
import { prisma } from '@/lib/prisma';
import { uuidv7 } from '@/lib/id';
import { recordAudit } from '@/server/audit/interceptor';

describe('foundation smoke (prisma + audit + id)', () => {
  beforeAll(async () => {
    // 确保库可连
    await prisma.$connect();
  });

  it('uuidv7 生成合法且版本为 7', () => {
    const id = uuidv7();
    expect(id).toHaveLength(36);
    expect(id.charAt(14)).toBe('7'); // 版本位
  });

  it('事务内写审计日志成功(before/after 为 JSONB)', async () => {
    const userId = uuidv7();
    await prisma.user.create({
      data: { id: userId, name: 'tester', role: 'BUDGET_ADMIN' },
    });
    const audit = await recordAudit(prisma, {
      objectType: 'test',
      objectId: userId,
      action: 'create',
      operatorId: userId,
      before: { amount: '0' },
      after: { amount: '100.50' },
    });
    expect(audit.afterData).toEqual({ amount: '100.50' });
    // 清理
    await prisma.auditLog.delete({ where: { id: audit.id } });
    await prisma.user.delete({ where: { id: userId } });
  });
});
```

- [ ] **Step 3: 跑冒烟测**

Run:
```bash
docker compose up -d postgres
npm test -- tests/server/foundation.smoke.test.ts
```
Expected: PASS(2 个用例全绿,说明 Prisma 连接、uuidv7、审计事务协同正常)。

- [ ] **Step 4: 跑全量测试 + 类型检查 + lint,做地基层总验收**

Run:
```bash
npm test
npm run check-types
npm run lint
```
Expected: 全部 PASS,无 error。

- [ ] **Step 5: Commit**

```bash
git add vitest.config.ts tests/server/foundation.smoke.test.ts
git commit -m "test: add foundation smoke integration test"
```

---

## 地基层完成标准(Definition of Done)

- [ ] `docker compose up -d postgres` 起库成功;`npm run dev` 起前端成功;紫色主题生效。
- [ ] Prisma schema 覆盖 §16 全部实体,`prisma migrate dev` 成功,生成 client。
- [ ] `lib/budget/` 公式库 occupancy/rate/adjustable/rollup 全部有单测且通过(覆盖 §4.4/4.5/7.4 关键口径,含除零、负值、作废不占用)。
- [ ] mock 鉴权 + RBAC 守卫可用,权限矩阵单测对齐 §2.2。
- [ ] 审计中间件可在事务内写 audit_logs,JSONB before/after 快照正确。
- [ ] `npm test` / `npm run check-types` / `npm run lint` 全绿。
- [ ] Husky pre-commit 在提交时触发 lint-staged。

地基层交付后,后续计划(预算核心层 / 业务与调整层 / 扩展层 / 收尾层)在此地基上构建。
